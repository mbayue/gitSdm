# Disable automatic .env file loading since Vite's loadEnv handles it
env = false

[test]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = [
  "coverage/**",
  "dist/**",
  "dist-server/**",
  "node_modules/**"
]